import styled from "styled-components"
//Animations
import { motion } from "framer-motion"

export const StyledHeroSection = styled(motion.section)`
  padding-top: 2rem;
  width: 100%;
  height: fit-content;
`
